# Журнал исправлений

## 6 февраля 2026

### Исправление 1: Проблема установки зависимостей

**Проблема**: Ошибки при установке зависимостей на Python 3.13/macOS
- `externally-managed-environment` - Python управляется через Homebrew
- `SSLError(SSLCertVerificationError('OSStatus -26276'))` - проблема с SSL сертификатами

**Решение**:
1. Создано виртуальное окружение `venv/`
2. Установка с флагами `--trusted-host` для обхода SSL
3. Обновлен `requirements.txt` с гибкими версиями (`>=` вместо `==`)
4. Созданы утилиты:
   - `install.sh` - автоматическая установка
   - `start.sh` - быстрый запуск
5. Добавлена документация:
   - `TROUBLESHOOTING.md`
   - `INSTALLATION_SUCCESS.md`

---

### Исправление 2: Отсутствие email-validator

**Проблема**: 
```
ImportError: email-validator is not installed
```

**Решение**:
- Установлен пакет `email-validator>=2.0.0`
- Добавлен в `requirements.txt`

---

### Исправление 3: Ошибка при регистрации с паролем 8 символов

**Проблема**: 
```
ValueError: password cannot be longer than 72 bytes, truncate manually if necessary
```

Ошибка возникала при инициализации bcrypt через passlib на Python 3.13. Passlib пытался запустить внутренний тест с длинным паролем, что вызывало падение.

**Решение**:
1. Заменили `passlib` на прямое использование `bcrypt`
2. Обновлен `app/core/security.py`:
   - Используется `bcrypt.hashpw()` напрямую
   - Используется `bcrypt.checkpw()` для проверки
   - Автоматическое обрезание пароля до 72 байт
3. Ограничена максимальная длина пароля до 72 символов в схемах

**Изменённые файлы**:
- `app/core/security.py` - прямое использование bcrypt
- `app/schemas/user.py` - `max_length=72` для паролей

**Результат**:
✅ Регистрация работает с паролями от 8 до 72 символов  
✅ Вход в систему работает корректно  
✅ Хеширование и проверка паролей работают  

---

### Исправление 4: JWT токен - "Subject must be a string"

**Проблема**:
```
Subject must be a string.
401 Unauthorized при попытке получить профиль
```

JWT стандарт требует, чтобы поле `sub` (subject) было строкой, а мы передавали integer (user.id).

**Решение**:
1. Изменён `app/api/v1/auth.py`:
   - `{"sub": str(user.id), ...}` вместо `{"sub": user.id, ...}`
2. Изменён `app/core/deps.py`:
   - Добавлено преобразование `user_id = int(user_id_str)`
   - Добавлена обработка ошибок преобразования

**Результат**:
✅ JWT токены создаются корректно  
✅ JWT токены валидируются корректно  
✅ Получение профиля работает  

---

## Протестировано

```bash
# Регистрация
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"email": "test@example.com", "username": "testuser", "password": "12345678", "full_name": "Test User"}'
# Результат: ✅ 201 Created

# Вход
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=testuser&password=12345678"
# Результат: ✅ 200 OK + JWT токен

# Получение профиля
curl -X GET "http://localhost:8000/api/v1/auth/me" \
  -H "Authorization: Bearer <token>"
# Результат: ✅ 200 OK + данные пользователя
```

---

## Текущий статус

✅ Все зависимости установлены  
✅ Сервер запускается без ошибок  
✅ Регистрация работает  
✅ Вход работает  
✅ Авторизация работает  
✅ База данных создана  

**Приложение полностью функционально!** 🎉
