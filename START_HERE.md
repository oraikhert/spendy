# 🚀 Добро пожаловать в Spendy!

## 👋 Начало работы

Этот проект - полнофункциональное приложение для отслеживания семейного бюджета на FastAPI.

### 📦 Шаг 1: Установка зависимостей

#### Вариант A: Автоматическая установка (рекомендуется)

```bash
./install.sh
```

Этот скрипт автоматически:
- Создаст виртуальное окружение
- Установит все зависимости
- Решит проблемы с SSL сертификатами (если есть)

#### Вариант B: Ручная установка

```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate  # Mac/Linux
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Если возникла ошибка SSL, используйте:
pip install --trusted-host pypi.org \
            --trusted-host pypi.python.org \
            --trusted-host files.pythonhosted.org \
            -r requirements.txt
```

⚠️ **Возникли проблемы?** Смотрите `TROUBLESHOOTING.md`

### ⚙️ Шаг 2: Настройка (опционально)

Файл `.env` уже создан с настройками по умолчанию для разработки.

Если нужно изменить настройки, отредактируйте `.env`:

```bash
nano .env
```

### 🏃 Шаг 3: Запуск приложения

#### Вариант A: Через скрипт (быстро)

```bash
./start.sh
```

#### Вариант B: Вручную

```bash
# Активируйте виртуальное окружение (если еще не активировано)
source venv/bin/activate

# Запустите приложение
python run.py
```

Приложение запустится на http://localhost:8000

🛑 **Остановка**: Нажмите `Ctrl+C`

### 📖 Шаг 4: Откройте документацию API

Откройте в браузере:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### 🧪 Шаг 5: Протестируйте API

В новом терминале (не закрывая сервер):

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите тесты
python test_api.py
```

## 📚 Документация

После установки изучите документацию:

| Файл | Описание |
|------|----------|
| **QUICKSTART.md** | Быстрый старт и основные команды |
| **README.md** | Полная документация проекта |
| **ARCHITECTURE.md** | Архитектура и дизайн приложения |
| **MIGRATIONS.md** | Работа с миграциями БД |
| **SUMMARY.md** | Сводка проекта и статистика |

## 🎯 Что можно делать?

### Через Swagger UI (http://localhost:8000/docs)

1. **Зарегистрироваться** → POST `/api/v1/auth/register`
2. **Войти** → POST `/api/v1/auth/login` (получить токен)
3. **Нажать "Authorize"** → вставить токен
4. **Получить профиль** → GET `/api/v1/auth/me`

### Через curl

```bash
# Регистрация
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "username": "testuser",
    "password": "password123",
    "full_name": "Test User"
  }'

# Вход (сохраните токен)
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=testuser&password=password123"

# Получить профиль (замените YOUR_TOKEN)
curl -X GET "http://localhost:8000/api/v1/auth/me" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 🗂️ Структура проекта

```
spendy/
├── app/                    # Основное приложение
│   ├── main.py            # Точка входа
│   ├── models/            # Модели БД
│   ├── schemas/           # Pydantic схемы
│   ├── api/v1/            # API endpoints
│   └── core/              # Утилиты
├── alembic/               # Миграции БД
├── test_api.py            # Тесты
└── README.md              # Документация
```

## ❓ Часто задаваемые вопросы

### Как остановить сервер?

Нажмите `Ctrl+C` в терминале, где запущен сервер.

### Где хранятся данные?

В файле `spendy.db` (SQLite) в корне проекта.

### Как сбросить базу данных?

```bash
# Остановите сервер, затем:
rm spendy.db
# При следующем запуске БД создастся заново
```

### Как перейти на PostgreSQL?

Смотрите инструкции в `README.md` или `MIGRATIONS.md`.

### Где посмотреть примеры запросов?

Откройте файл `api_examples.http` или используйте Swagger UI.

## 🐛 Проблемы?

### Ошибка "externally-managed-environment"
Решение: Используйте виртуальное окружение (шаг 1)

### Ошибка SSL сертификатов
Решение: Запустите `./install.sh` или установите с флагом `--trusted-host`

### Порт 8000 занят
Решение: Измените порт в `run.py` на 8001

### Модуль не найден
Решение: Активируйте виртуальное окружение: `source venv/bin/activate`

### Подробное решение всех проблем
📖 Смотрите **`TROUBLESHOOTING.md`** - там решения для всех типичных проблем!

## 🎉 Готово!

Вы готовы к разработке! Следующие шаги:

1. ✅ Изучить код в `app/`
2. ✅ Прочитать документацию
3. ✅ Создать свои модели и endpoints
4. ✅ Развивать функционал бюджета

**Удачной разработки!** 🚀

---

💡 **Подсказка**: Начните с изучения `QUICKSTART.md` для быстрого погружения в проект.
